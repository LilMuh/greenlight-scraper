# Copy to .env and fill in. .env is gitignored.
PORT=8090

# GreenLight talks to a fingerprint browser service that exposes the Chrome
# DevTools Protocol, to reach booking sites behind anti-bot protection.
# Point these at your own service (see README).
BROWSER_SERVICE_URL=
BROWSER_PROFILE_ID=
BROWSER_API_KEY=
